from utils import *
import pandas as pd
import numpy as np

## Function to find N number of similar users for a given input user.
# def find_similar_users(userid, N):
#    top_n_users = similar_df.loc[userid][:N]
#    return top_n_users

## read the similarity score matrix from the pickle file
# similar_df = pd.read_pickle(data_dir + 'similar_df_master.pickle')

# find similar users
# find_similar_users(userid, N)
